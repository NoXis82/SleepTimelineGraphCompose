package ru.noxis.sleeptimelinegraphcompose.model

import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.geometry.Size

data class SleepStageDrawPoint(
    val topLeft: Offset,
    val size: Size
)
